- **Advanced Space Management & Foldable Automations**: Create, name, color-code, and reorder home spaces. Configure dynamic screen posture rules (`Folded`, `Unfolded`, `Flex Mode`, `Compact/Medium/Expanded`, `Orientation`, `External Display`) to automatically switch spaces, set posture-specific default spaces, or assign custom pie menu touch layouts per space.
- **HTTP Query Variables, Subquery Variants & Template Interpolation**: Use `${var}` or `{{var}}` placeholders in HTTP query URLs, headers, and request bodies. Define variable presets, subquery variants (e.g. `{http:query_name:variant_name}`), and prompt dialogs on tap when triggering actions with missing parameters.
- **Recently Dismissed Notification Trash**: Browse notifications dismissed within the last 24 hours in the Inbox hub. Permanently delete dismissed items, empty trash, or save them directly to your Catch-Up reading list.
- **Media Playback & Volume Actions**: Control music and podcast playback with dedicated actions for Play/Pause, Play, Pause, Next Track, Previous Track, Volume Up, Volume Down, and Mute Toggle on pie slices, action icons, or gesture triggers.
- **Crash Recovery & Safe Mode**: Built-in crash-loop detection automatically engages Safe Mode to suppress crashing widgets, allowing quick access to settings and app launching. A standalone Emergency Recovery screen lets you easily reset widgets or switch default launchers.
- **Tenkai Capsule Padding & Formula Engine Bounds**: Add custom content padding inside Tenkai Capsules. The formula parser now safely handles both single and word boolean operators (`&&`, `&`, `AND`, `||`, `|`, `OR`, `==`, `=`) with execution iteration limits.